package com.you.visionaid

import org.junit.Test

import org.junit.Assert.*

/**
 * Android Studio 创建工程时生成的基础 JVM 单元测试示例。
 *
 * 该测试在开发机上执行，不依赖模拟器或 Android Framework。
 */
class ExampleUnitTest {
    @Test
    fun addition_isCorrect() {
        // 最小断言用于确认本地 JUnit 测试环境配置正常。
        assertEquals(4, 2 + 2)
    }
}
